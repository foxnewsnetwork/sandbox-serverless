type Query {
	meInfo(consumer_key: String!, consumer_secret: String!): User!
}

type User {
	name: String!
	handle: String!
	location: String!
	description: String!
	followers_count: Int!
	friends_count: Int!
	favourites_count: Int!
	following: [String!]!
}

schema {
	query: Query
}
